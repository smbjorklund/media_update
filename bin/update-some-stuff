#!/bin/sh

for u in $(sed 's/#.*//' etc/vip.txt); do
    bin/site-drush --verbose uib-sync-w2-user $u
    bin/site-drush --verbose uib-sebra-user $u
done

bin/site-drush --verbose uib-sebra-areas
bin/site-drush --verbose uib-sync-fs
bin/update-norsk
